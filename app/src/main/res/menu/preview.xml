<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".filescan.ScannerActivity">
    <item android:id="@+id/action_revert_preview"
        android:title="Undo"
        android:icon="@drawable/ic_action_undo"
        android:orderInCategory="100"
        app:showAsAction="always" />

    <item android:id="@+id/action_done_preview"
        android:title="Finish"
        android:icon="@drawable/ic_action_done"
        android:orderInCategory="100"
        app:showAsAction="always" />

    <item android:id="@+id/action_add_preview"
        android:title="Add Document"
        android:icon="@drawable/ic_action_adddoc"
        android:orderInCategory="100"
        app:showAsAction="always" />
</menu>
